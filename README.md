`tf-aws-vpc` creates AWS multi-AZ vpc with public and private subnets, NAT gateways, ssh bastion host

`tf-aws-ecs` creates AWS ECS cluster, auto-scaling group, iam instance profiles, application load balancer 
